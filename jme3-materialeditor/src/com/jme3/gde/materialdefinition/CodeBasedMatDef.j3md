MaterialDef ${name} {

    MaterialParameters {
        Vector4 Color
    }

    Technique {
        VertexShader GLSL100: ${name}.vert
        FragmentShader GLSL100: ${name}.frag

        WorldParameters {
            WorldViewProjectionMatrix
        }
    }

}